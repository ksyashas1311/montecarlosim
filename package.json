{
  "name": "fintwin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "backend": "cd backend && ../engine/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "test": "engine/.venv/bin/pytest backend/tests/ -v"
  }
}
